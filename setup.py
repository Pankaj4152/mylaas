from setuptools import setup, find_packages

setup(
    name="text_image_toolkit",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="A modular library for text and image preprocessing.",
)